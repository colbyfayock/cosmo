{
  "name": "@cosmo/workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test"
  },
  "dependencies": {
    "@cosmo/lib": "link:lib"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
